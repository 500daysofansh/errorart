from setuptools import setup, find_packages

setup(
    name="errorart",
    version="0.1.0",
    description="🎨 Make Python errors fun with ASCII art, haikus, and sarcasm.",
    author="Your Name",
    packages=find_packages(),
    install_requires=[],
    python_requires=">=3.6",
)
